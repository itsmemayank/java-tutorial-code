import java.lang.ProcessBuilder.Redirect.Type;

import javax.swing.plaf.TreeUI;

public class Run {
    public static void main(String[] args) {

        // Wrapper Class - provides a way to use primitive datatypes as reference
        // datatypes.
        // Reference datatypes contain useful methods can be used with collections.

        // Autoboxing - the automatic conversion that the java compiler makes between
        // the primitive types and their corresponding object wrapper classes.

        // Unboxing - the automatic conversion of wrapper class to primitive class.

        Boolean a = true;
        Character b = 'a';
        Integer c = 123;
        Double d = 3.14;

        System.out.println(a);
        System.out.println(b);
        System.out.println(c);
        System.out.println(d);

    }
}
