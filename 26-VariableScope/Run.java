
public class Run {
    public static void main(String[] args) {

        // Local - declared inside method (visible only to that method)
        // Global - declared outside the method, but within the class
        // (visible to all parts of a class)

        DiceRoller diceRoller = new DiceRoller();
    }
}